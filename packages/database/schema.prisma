generator client {
  provider = "prisma-client-js"
  output   = "./generated"
}

datasource db {
  provider = "postgresql"
}

model Event {
  id           String   @id
  type         String
  receivedAt   DateTime
  processedAt  DateTime?
  properties   Json?
}